.DS_Store
node_modules
/build
/.svelte-kit
/package
# Build output. Not in .gitignore's sight for these two, so running the linter
# after a build used to report 35 generated files as "code style issues".
.vercel
.output
.env
.env.*
!.env.example

# Ignore files for PNPM, NPM and YARN
pnpm-lock.yaml
package-lock.json
yarn.lock

# 4.8MB of generated chart data — not source, and reformatting it would
# rewrite the whole file.
src/lib/data/cc2000_data.json
